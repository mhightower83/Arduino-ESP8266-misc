recipe.hooks.prebuild.01.pattern=echo You need to edit "platform.local.txt" and replace "**YourUserId**" with your user id.
#
#                Attention!
#
# Delete the above lines and do edits below. Replace **YourUserId** with a value that
# will result in custom.globalh.path pointing to your Sketchbook Arduino/include directory.
# If you are not using default file locations, make other updates as need.

custom.userid=**YourUserId**



custom.globalh.path=/home/{custom.userid}/Arduino/include
custom.globalh.path.windows=C:\Users\{custom.userid}\Documents\Arduino\include
custom.gnutool.path=/bin
# Assumes GIT is installed on Windows systems.
custom.gnutool.path.windows=C:\Program Files\Git\usr\bin

compiler.c.extra_flags=-include "{custom.globalh.path}/ide_global.h" -include "{build.path}/sketch/global.h"
compiler.S.extra_flags=-include "{custom.globalh.path}/ide_global.h" -include "{build.path}/sketch/global.h"
compiler.cpp.extra_flags=-include "{custom.globalh.path}/ide_global.h" -include "{build.path}/sketch/global.h"

# Copy a sketch/global.h template file into the build tree if it is missing.
# This file is a default. If you have one in your sketch's source directory,
# the Arduino IDE build process will replace the default.
recipe.hooks.prebuild.02.pattern="{custom.gnutool.path}/mkdir" -p "{build.path}/sketch"
recipe.hooks.prebuild.03.pattern="{custom.gnutool.path}/cp" -n --preserve=timestamps "{custom.globalh.path}/global.h" "{build.path}/sketch/global.h"
